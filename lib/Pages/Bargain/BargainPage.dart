import 'package:flutter/material.dart';

class NewBargain extends StatelessWidget {
  const NewBargain({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
